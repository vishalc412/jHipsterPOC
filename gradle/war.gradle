apply plugin: "war"

bootWar {
    mainClassName = "com.org.jhipsterpoc.JHipsterMspocApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
